spring:
  application:
    name: spring-cloud-apollo-client
server:
  port: 8762
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
management:
  security:
    enabled: false

app:
  id: spring-cloud-apollo-client

apollo:
  meta: http://localhost:8080
